<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnCLickRecyclerView"
        tools:targetApi="31">
        <activity
            android:name=".DeviceConnectionActivity"
            android:exported="false"
            android:label="@string/title_activity_device_connection"
            android:theme="@style/Theme.OnCLickRecyclerView" />
        <activity
            android:name=".ui.theme.WIfiSettings"
            android:exported="false" />


        <activity
            android:name=".ui.theme.Settings"
            android:exported="false" />
        <activity
            android:name=".ui.theme.DeviceAdd"
            android:exported="false" />
        <activity
            android:name=".sensorDetails"
            android:exported="false"
            android:label="@string/title_activity_sensor_details"
            android:theme="@style/Theme.OnCLickRecyclerView" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.OnCLickRecyclerView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>